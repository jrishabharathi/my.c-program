#include <stdio.h>
int main() {
    int x = 5;
    printf("Initial value of x: %d\n", x);
    x++;
    printf("After increment (x++): %d\n", x);
    x--;
    printf("After decrement (x--): %d\n", x);
    return 0;
}
